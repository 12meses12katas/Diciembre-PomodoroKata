import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class ReadyTest {

    @Test public void
    readyToTestDriveMyDevelopment() {
        assertThat(true, is(true));
    }
}
